<h1>Bands!</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
 
<% @bands.each do |band| %>
  <tr>
    <td><%= band.title %></td>
    <td><%= link_to "Show", band %></td>
    <td><%= link_to "Edit", edit_band_path(band) %></td>
    <td><%= link_to "Remove", band, method: :delete, data: { confirm: "Are you sure?" } %></td>
  </tr>
<% end %>
</table>
 
<br>
 
<%= link_to "New Band", new_band_path %>
